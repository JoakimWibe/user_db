import { Box, Heading } from "@chakra-ui/react";

const Navbar = () => {
  return (
    <Box background={"cadetblue"} padding={4} textAlign={"center"}>
      <Heading color={"white"}>User Database</Heading>
    </Box>
  );
};

export default Navbar;
